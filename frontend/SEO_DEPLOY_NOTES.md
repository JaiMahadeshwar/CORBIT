# CONTROLORBIT SEO deployment notes

Included:
- `public/sitemap.xml`
- `public/robots.txt`
- upgraded homepage metadata in `index.html`

After redeploying:

1. Open `https://www.controlorbit.com/sitemap.xml`
   - It must show XML text, not the homepage.
2. Open `https://www.controlorbit.com/robots.txt`
   - It must show the sitemap line.
3. In Google Search Console, submit:
   - `sitemap.xml`
4. Use URL Inspection and request indexing for:
   - `https://www.controlorbit.com/`

Next SEO growth step:
Add real `/insights` pages targeting project controls, cost estimating, schedule risk and space infrastructure keywords.