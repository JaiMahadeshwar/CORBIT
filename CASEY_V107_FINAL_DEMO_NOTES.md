# CASEY V107 Final Demo Build

Includes:
- one credible Earth or Space public demo run per email
- one demo export set per run/email
- export gating with paid-access message after the free export set
- strong P6/PRA-style XER export via the dynamic v64 schedule engine
- demo watermarking/stamping in exported pack metadata/readme/model
- QCRA/QSRA workbook with curves and tornado drivers
- cost workbook, risk register, XER, schedule CSV, QCRA/QSRA, JSON audit and full pack exports
- selected scenario text fixed to black on cyan

Run backend from `backend`, frontend from `frontend`.
