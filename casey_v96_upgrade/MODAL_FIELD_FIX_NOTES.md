# Modal Field Fix

This build fixes the public demo modal issue where Windows/browser native dropdown styling appeared as a large white box and hid the form fields.

Fixes added:
- dark select styling
- visible input/textarea/select fields
- stronger z-index controls
- pointer events restored
- placeholder and caret color corrected
- browser dropdown styling neutralized

Use this ZIP instead of the previous one.